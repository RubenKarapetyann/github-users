// import { UsersList } from "../../../types/api"
// import card from "../../card/card.mjs"
export {};
// export const renderUsers = (list: UsersList, parent: HTMLDivElement) => {
//     list.forEach((user, i) => {
//         const currentCard = card({
//             login : user.login,
//             avatar_url : user.avatar_url,
//             id : user.id
//         })
//         parent.appendChild(currentCard)
//     }) 
// }
